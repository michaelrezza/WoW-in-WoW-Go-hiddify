name: Auto Update WARP in WARP

on:
  schedule:
    - cron: '0 * * * *'   # Runs every hour
  workflow_dispatch:        # Manual trigger

jobs:
  update_warp:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Install Dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y wireguard-tools curl jq fping

      - name: Download and Install WARP-GO
        run: |
          wget -O install-warp.sh https://raw.githubusercontent.com/Ptechgithub/warp/main/endip/install.sh
          chmod +x install-warp.sh
          sudo ./install-warp.sh

      - name: Ensure Scripts Are Downloaded & Executable
        run: |
          mkdir -p ./scripts
          
          if [ ! -f ./scripts/warp-go.sh ]; then
            echo "⚠️ warp-go.sh not found! Downloading..."
            wget -O ./scripts/warp-go.sh https://raw.githubusercontent.com/Ptechgithub/warp/main/scripts/warp-go.sh
          fi
          
          if [ ! -f ./scripts/ipv6-scan.sh ]; then
            echo "⚠️ ipv6-scan.sh not found! Downloading..."
            wget -O ./scripts/ipv6-scan.sh https://raw.githubusercontent.com/Ptechgithub/warp/main/scripts/ipv6-scan.sh
          fi
          
          if [ ! -f ./scripts/update-ip-port.py ]; then
            echo "⚠️ update-ip-port.py not found! Downloading..."
            wget -O ./scripts/update-ip-port.py https://raw.githubusercontent.com/Ptechgithub/warp/main/scripts/update-ip-port.py
          fi
          
          chmod +x ./scripts/*.sh || echo "⚠️ No executable files found!"

      - name: Run WARP Setup
        run: |
          echo "✔️ Running warp-go.sh..."
          for i in {1..3}; do
            echo "2" | bash ./scripts/warp-go.sh && break || echo "❌ warp-go.sh failed! Retrying ($i)..."
            sleep 5
          done

      - name: Run IPv6 Scanner
        run: |
          echo "✔️ Running ipv6-scan.sh..."
          for i in {1..3}; do
            bash ./scripts/ipv6-scan.sh && break || echo "❌ ipv6-scan.sh failed! Retrying ($i)..."
            sleep 5
          done

      - name: Update Hiddify Config
        run: |
          echo "✔️ Running update-ip-port.py..."
          for i in {1..3}; do
            python3 ./scripts/update-ip-port.py && break || echo "❌ update-ip-port.py failed! Retrying ($i)..."
            sleep 5
          done

      - name: Commit and Push Changes
        run: |
          git add hiddify-config.json || echo "⚠️ No files to update."
          git commit -m "🔄 Auto-update fastest WARP in WARP IPv6" || echo "⚠️ No changes to commit."
          git push || echo "⚠️ Failed to push changes."

      - name: Auto-Restart on Failure
        if: failure()
        run: |
          echo "❌ Workflow failed! Restarting..."
          gh workflow run update-warp.yml
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}