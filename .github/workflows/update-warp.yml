name: Auto Update WARP in WARP

on:
  schedule:
    - cron: '0 * * * *'   # Runs every hour
  workflow_dispatch:        # Manual trigger

jobs:
  update_warp:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Install Dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y wireguard-tools curl jq fping

      - name: Download and Install WARP-GO
        run: |
          wget -O install-warp.sh https://raw.githubusercontent.com/Ptechgithub/warp/main/endip/install.sh
          chmod +x install-warp.sh
          sudo ./install-warp.sh

      - name: Ensure Scripts Are Executable
        run: |
          chmod +x ./scripts/warp-go.sh ./scripts/ipv6-scan.sh ./scripts/update-ip-port.py || echo "⚠️ No executable files found!"

      - name: Run WARP Setup
        run: |
          if [ -f ./scripts/warp-go.sh ]; then
            echo "✔️ warp-go.sh found. Running setup..."
            for i in {1..3}; do
              echo "2" | bash ./scripts/warp-go.sh && break || echo "❌ warp-go.sh failed! Retrying ($i)..."
              sleep 5
            done
          else
            echo "❌ warp-go.sh not found! Please make sure the file exists."
            exit 1
          fi

      - name: Run IPv6 Scanner
        run: |
          if [ -f ./scripts/ipv6-scan.sh ]; then
            echo "✔️ ipv6-scan.sh found. Running IPv6 scan..."
            bash ./scripts/ipv6-scan.sh || { echo "❌ ipv6-scan.sh failed! Restarting..."; bash ./scripts/ipv6-scan.sh; }
          else
            echo "❌ ipv6-scan.sh not found! Please make sure the file exists."
            exit 1
          fi

      - name: Update Hiddify Config
        run: |
          if [ -f ./scripts/update-ip-port.py ]; then
            echo "✔️ update-ip-port.py found. Running update..."
            python3 ./scripts/update-ip-port.py || { echo "❌ update-ip-port.py failed! Restarting..."; python3 ./scripts/update-ip-port.py; }
          else
            echo "❌ update-ip-port.py not found! Please make sure the file exists."
            exit 1
          fi

      - name: Commit and Push Changes
        run: |
          git add hiddify-config.json || echo "⚠️ No files to update."
          git commit -m "🔄 Auto-update fastest WARP in WARP IPv6" || echo "⚠️ No changes to commit."
          git push || echo "⚠️ Failed to push changes."

      - name: Auto-Restart on Failure
        if: failure()
        run: |
          echo "❌ Workflow failed! Restarting..."
          gh workflow run update-warp.yml
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}